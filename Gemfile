source "https://rubygems.org"

# GitHub Pages builds your site with this gem; it pins Jekyll and the
# plugins GitHub supports. Keeps local previews identical to production.
gem "github-pages", group: :jekyll_plugins

gem "jekyll-feed", "~> 0.17"
gem "webrick", "~> 1.8"   # needed for local preview on Ruby 3.x
